{
  "name": "@platform/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase Auth utilities — client-safe helpers only",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "node --test dist/index.test.js",
    "test:unit": "pnpm test",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@platform/types": "workspace:*",
    "@supabase/supabase-js": "^2.43.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  }
}
