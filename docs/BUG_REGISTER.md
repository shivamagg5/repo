# Bug Register

This queue is intentionally implementation-neutral. It identifies the current behavior and acceptance criteria; no fixes were made during the audit.

| ID | Title | Severity | Root cause / current behavior | Exact files likely involved | Tests and acceptance criteria |
| --- | --- | --- | --- | --- | --- |
| FIX-001 | Block mock-payment self-confirmation | P0 | Client controls `provider`; mock gateway is available in all environments and uses known literal signature. A buyer can obtain paid tickets without Razorpay. | `payments.service.ts`, `mock-payment.gateway.ts`, validation, module/config | Production rejects mock intent/webhook; test-only adapter cannot be reached in prod; real gateway provider cannot be supplied by client. |
| FIX-002 | Process only terminal successful payment events | P0 | Webhook verifies signature/amount but does not require `payment.captured`/equivalent success state. | `payments.service.ts`, `razorpay-payment.gateway.ts` | Signed `payment.failed`, authorized, refund, and unknown events never pay/order/issue; captured fixture succeeds once. |
| FIX-003 | Fail closed for scanner keys | P0 | Production warns then uses generated deterministic keys; existing test exposes it. | `scanner-crypto.service.ts`, env docs, tests | Missing/invalid production key material prevents startup; key rotation/version tests pass. |
| FIX-004 | Implement actual mobile ECDSA verification | P0 | Scanner `CryptoService` returns structural checks instead of cryptographic verification. | `apps/scanner-mobile/lib/core/crypto_service.dart` | Known valid package/ticket passes; mutated payload/signature/root/key-version/expiry all fail offline. |
| FIX-005 | Enforce scanner device and staff scope | P0 | Guard not applied; key is discarded; controller uses wrong `user.sub` fallback; service has no assignment checks. | scanner controller/service/guard/schema/migration, device key service | Revoked/unassigned/cross-org/cross-event device and staff are denied; valid assigned device succeeds. |
| FIX-006 | Remove fake scanner admission paths | P0 | Pair/device/online errors create fake IDs/packages and accept then queue scans; fixed gates/events and demo credentials ship. | scanner provider, pairing screen, auth service, attendee lookup | Network/pair failure leaves device unpaired and cannot admit; test fixtures are build-flavor/test-only. |
| FIX-007 | Align scanner response envelope and sync DTO | P0 | Client reads unwrapped fields; backend global interceptor wraps them; sync response fields differ. | scanner API service/provider, API DTO/types, backend controller/service | Pair, online scan, offline sync, replay and conflict paths tested against running API. |
| FIX-008 | Restore buildable public event contract | P0 | `toPublicEventDetail` omits required ticket types. | `events.service.ts`, types/client tests | Backend `build` and public-event contract tests pass. |
| FIX-009 | Establish one deployed migration/RLS path | P0 | Docs/manual SQL and Drizzle point at different schema histories; deploy has no migration job. | `drizzle.config.ts`, migration dirs, `database/README.md`, CI/render config | Clean DB and staging upgrade deterministically reach same 57-table schema/RLS posture; migration is recorded. |
| FIX-010 | Make payment/refund accounting authoritative | P0 | Paid webhooks/refunds do not write ledger; refund does not contact provider/create refund record/void tickets. | payments, admin, refunds, finance ledger, tickets, schema | Atomic paid/refund posting balances debits/credits, has idempotency, provider state and ticket/inventory reconciliation. |
| FIX-011 | Scope settlements and analytics to organization/event | P0 | Settlement and organizer analytics aggregate globally; finance permission for generation is only `finance.view`. | settlements, analytics, controller permissions | Two-org fixture proves no cross-org amounts/read access; generation needs dedicated permission. |
| FIX-012 | Use integer-safe financial arithmetic | P0 | `number`, `Math.round`, `0.1` and `0.18` calculate monetary values. | schema, finance/settlements/commissions/payment DTOs | Minor-unit/basis-point calculations avoid float and overflow; large-value boundary tests pass. |
| FIX-013 | Repair Flutter order envelope parsing | P0 | `getOrder` and confirm parse the envelope as an order; hold flow suppresses error. | consumer `api_service.dart`, ticketing provider, checkout/order screens | Device/integration test completes reservation → order → intent and surfaces typed errors. |
| FIX-014 | Remove payment gateway fallback order IDs | P1 | Razorpay API/network failure returns a synthetic provider ID instead of an error. | `razorpay-payment.gateway.ts`, payments service | Provider failure is explicit and no payment transaction/order checkout is emitted. |
| FIX-015 | Add/operate hold-expiry worker | P1 | Hold expiry service source/tests exist, but no scheduler/worker registration or deployment evidence was found. | inventory module, worker/bootstrap/deploy config | Expired holds release exactly once under real PostgreSQL job execution. |
| FIX-016 | Implement durable notification processing | P1 | No worker schedule; claim does not use SKIP LOCKED; failed rows are not retryable/backed off; no domain events enqueue. | notification outbox/service/providers, worker config | Concurrent workers claim safely; exponential retry/dead letter behavior works; paid/refund/cancel emits notifications. |
| FIX-017 | Complete promoter campaign detail contract | P1 | Page calls missing `GET /promoter/campaigns/:id`. | promoter controller/service, api client, page | Owner sees campaign; other promoter gets 403/404. |
| FIX-018 | Remove production demo/fallback user data | P1 | Consumer local demo session, fixed categories, and `ord_` confirmation bypass remain. | consumer mobile auth/home/checkout | Production build has no false authenticated/payment success paths. |
| FIX-019 | Replace unscoped mobile direct HTTP | P1 | Multiple Flutter services bypass typed client; scanner fails envelope semantics. | mobile API/analytics/scanner services | Contract tests cover all requests, auth headers, envelopes and non-2xx responses. |
| FIX-020 | Add indexes and schema constraints for operational queries | P1 | No ordinary indexes in active Drizzle schema; queue/hold/checkin queries grow unbounded. | schema/migration, query plans | Indexes selected from EXPLAIN on realistic fixtures; counter/status constraints prevent invalid values. |
| FIX-021 | Harden CMS lifecycle and public validation | P2 | Sanitizer is regex-only; event ID validation accepts absent references until insert; edit/delete/public collection/cache workflows absent. | CMS service/controller/schema | Strict schema/URL policy and full lifecycle tests. |
| FIX-022 | Replace hardcoded analytics metrics | P2 | Organizer metrics unscoped and scanner/admin metrics hardcode values (12%, 18%, 5, 12, 100%). | analytics service, dashboard UI | Metrics use scoped authoritative queries and reconciliation tests. |
| FIX-023 | Re-run visual/accessibility QA against executable builds | P2 | Prior visual report is not reproducible evidence; backend build currently prevents full test environment. | UI routes, visual QA tooling | Captured viewport/device artifacts plus keyboard/screen-reader checks tied to a build hash. |

